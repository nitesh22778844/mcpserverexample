
to Istall the add_server use below cammand in config file
```json
{
     "mcpServers": {
     "add_tool_mcp_server_git": {
      "command": "uvx",
      "args": [
		"--from",
		"git+https://github.com/nitesh22778844/mcpserverexample.git",
		"mcp-server"
      ]
    }
}
}

```